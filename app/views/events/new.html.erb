<html>
    <head>
        <title> EventBrite </title>
    </head>
    <body>
    <br><h2>Trop cool ! Tu veux créer un nouvel événement</h2>
    <br><h3>Remplis donc le formulaire ci dessous :) </h3>
    
    <%= form_for @event do |f| %>

      <div class="form-group">
        <%= f.label :title, "Titre" %>
          <%= f.text_field :title %>
      </div>

      <div class="form_group">
        <%= f.label :description, "Description" %>
        <%= f.text_area :description %>
      </div>

      <div class="form-group">
        <%= f.label :start_date, "Start-date" %>
        <%= f.date_field :start_date %>
      </div>

      <div class="form-group">
        <%= f.label :duration, "Duration" %>
        <%= f.datetime_field :duration %>
      </div>


      <div class="form-group">
       <%= f.label :location, "Location" %>
       <%= f.text_field :location %>
      </div>

      <div class="form-group">
        <%= f.label :price, "Price" %>
        <%= f.text_field :price %>
     </div>

    <%= form.submit "Créer l'event", class: 'btn btn-lg btn-primary btn-block' %>
  <% end %>

  </body>
  </html>